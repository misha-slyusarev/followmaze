#!/usr/bin/env ruby

lib = File.expand_path(File.dirname(__FILE__) + '/../lib')
$LOAD_PATH.unshift(lib) if File.directory?(lib) && !$LOAD_PATH.include?(lib)

require 'proxy'

args = ARGV.dup
ARGV.clear

if args
  events_port = args.shift.strip
  Proxy.new.run events_port
else
  Proxy.new.run
end
